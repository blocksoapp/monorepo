import React from 'react'

function WalletFeed() {


     /*
    const handler = async () => {
        try {
            const res = await fetch(walletfeed)
            const json = await res.json()
            console.log(json.address)
            setWallets(json.address)
        } catch (error) {
            console.log(error)
        }
    } 

    useEffect(() => {
      handler()
    
    }, [])

    */

    
  return (
    <div>WalletFeed</div>
  )
}

export default WalletFeed